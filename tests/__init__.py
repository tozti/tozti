"""
This file does nothing and allow us to run `pytest tests/` 
in order to execute the tests. Otherwise, we would have to 
play with pythonpath, by doing `export PYTHONPATH=.` inside the
main directory for example.
"""
